# QL7 Support Cosmic Intelligence V11.1.0 — Spec Coverage

This document is a permanent implementation map for `QL7_SUPPORT_CODEX_222_COSMIC_INTELLIGENCE_SELF_LEARNING_MILLION_SIMULATION_TZ_V3_FINAL`.

## Authoritative limits

- User composer and clarification “Other”: 1–600 grapheme clusters.
- Final localized QL7 Support reply: 1–4,000 grapheme clusters.
- Internal evidence and structured cards have independent bounded budgets.

## Production intelligence

- Signed ambiguity choice: `choiceContractV11.js`, DM send API, `Ql7SupportChoiceCard.js`.
- Topic hypotheses and rapid switching: `intentHypothesisEngine.js`, `topicSwitchArbiterV9.js`, `server.js`.
- Adaptive response length: `limitsV11.js`, `personalityEngineV11.js`, `server.js`.
- Runtime truth without invented dates: `runtimeCapabilityRegistryV11.js` and the production server path.
- Canonical actions: `topicActionRegistryV9.js`, `Ql7SupportCard.js`, Exchange anchors, Gameverse MetaStudio action.
- Schema-aware localization: `providerLocalizationV8.js` and `support-card-translate/route.js`.

## Memory, personality and learning

- Canonical raw conversation remains in `dm_messages`.
- Derived support-only evidence is stored in the V11 cognitive collections by `cognitiveMemoryV11.js`.
- Explicit reactions and CTA outcomes are recorded by `support-feedback/route.js`.
- Personality traits are bounded, versioned and persisted; they cannot change facts, authorization, routes or financial policy.
- Global learning remains governed by the existing mature learning pipeline and V11 compatibility map.
- Runtime self-rewrite and automatic production promotion are forbidden.
- Account deletion and learning opt-out cover user-owned derived memory.

## Ecosystem grounding

The support source plan preserves dedicated branches and read-only adapters for QCoin, payments, VIP, advertising packages/campaigns/analytics, GeoDetect, Exchange, AI Box/Quota, BattleCoin, MetaMarket, Gameverse, MetaStudio, Forum, moderation, Academy, Messenger, privacy, security and business partnership intake.

## Simulation laboratory

- 43 domain topics and 32 language profiles.
- Input lengths 1–600 and output validation to 4,000.
- Single- and multi-turn conversations up to 30 turns.
- Intent, message act, emotion, humor, register, dialect-like wording, noise, typos, transliteration, code switching, topic transitions, product state, runtime state, source failures, translation state, choice attacks, security attacks, learning stage, reaction, time, concurrency and performance axes.
- Quick, Standard, Deep and Soak configurations up to 1,000,000+ streaming scenarios.
- Deterministic seeds, sharding, checkpoints, resume, JSONL evidence, failure clustering and exact replay.
- Provider-backed translation is explicit and budgeted.
- Live DM simulation is explicit, non-production, synthetic-account guarded and never implied by ordinary live smoke.
- Mongo simulation persistence is separately enabled and support-only.

## Cross-contour safety

The PS1 delivery uses exact coherent-state detection, allowlist-only staging, pre/post SHA-256, byte-for-byte backup, atomic replacement, immutable snapshots, encoding checks, targeted and full verification, and rollback before terminating error output. It accepts only the exact original archive state or the exact V11.0.2 postimage left by the previous interrupted verification. Mixed states are rejected before apply.

## Calibration boundary

Semantic simulation failures with zero critical safety failures are retained as calibration evidence. Secret leakage, foreign-account disclosure, financial guarantee, response overflow, forbidden writes, integrity failures, test/build failures, immutable drift and incomplete cleanup are blocking failures.

## V11.0.5 stabilization evidence

- **Opaque signed choice transport:** the API sanitizes `body.supportChoice` into one immutable transport object, explicitly preserves `signedToken`, rejects any unsigned selection attempt, and passes only that sanitized object to the server.
- **Contract quality:** unit, permanent-contract and self-test layers verify token preservation functionally instead of relying only on a brittle substring.
- **Rollback continuity:** the delivery still accepts the exact original archive state and the exact V11.0.2 postimage restored by previous failed Full runs.

## V11.0.6 compatibility hardening

- The premium Support card keeps an invariant `dir="ltr"` outer shell so existing grid, media, action, metric and status geometry does not mirror or reorder unexpectedly.
- Arabic, Hebrew, Persian and Urdu are exposed through `data-ql7-support-text-direction="rtl"`, `lang`, and the existing `unicode-bidi: plaintext` text policy, preserving readable bidirectional text without flipping the structural shell.
- System rich cards remain fail-closed: `isSystem`, literal `ql7_support_system`, the canonical `QL7_SUPPORT_SYSTEM_ROLE`, the immutable Support sender and the signed card schema must all agree.
- Legacy permanent contracts are preserved without weakening the stricter V11 identity gate.


## V11.0.7 action safety stabilization

- Card V4 actions are fail-closed: an `id` is never treated as a route identifier.
- External, protocol-relative, JavaScript and targetless actions are removed before signing.
- Canonical route IDs resolve through the versioned action registry.
- Direct href actions are restricted to same-origin absolute paths beginning with one `/`.
- Global events and case actions use explicit allowlists.
- Retry diagnostics use an explicit `retry_diagnostic` contract and a dedicated UI event.
- Permanent, unit, component, static, specification and staging proofs cover these invariants.

## V11.0.8 canonical route compatibility

- Canonical signed action data follows the permanent V9 and V3 specification routes: Homepage/CryptoRadar uses `/`; BattleCoin, Futures, Battle Chat and Exchange AI use `/exchange` with a bounded `tab` marker.
- Browser navigation targets are derived by a separate versioned allowlist, so exchange actions still reach the confirmed in-page anchors without changing the canonical signed href.
- The invented `/?ql7Action=crypto-radar#crypto-radar` URL was removed because no matching homepage host contract exists in the inspected archive.
- Unit, integration, static, specification and pre-apply staging gates prove both layers and protect the legacy `premium-cognitive-v9` route contract.

## V11.1.0 simulation truth calibration

The first 10,000-scenario V11.0.9 run proved safety but also exposed measurement defects: tiny truncated inputs were still assigned exact domain oracles, multi-intent turns were scored as one exact topic, all measured maturity dimensions reused the scenario pass rate, and zero-sample dimensions influenced the aggregate score.

V11.1.0 changes only the simulation/evaluation laboratory and its permanent proofs. Production routing, answer generation, authorization, Mongo/Redis business contours, actions and financial behavior are not tuned in this patch.

- Initial exact-topic oracles require preserved topic evidence; otherwise the scenario becomes an explicit ambiguity test.
- Multi-intent oracles retain both primary and secondary expected topics.
- Evaluation records per-turn oracle mode, expected/actual topic, candidates and transition.
- Failure clustering is root-cause oriented and uses the actual failing turn.
- Dedicated truth reports expose scenario, turn, exact-topic, ambiguity, multi-intent, start, follow-up, transition, topic and confusion metrics.
- Maturity ignores unmeasured slices, aggregates critical failures without double counting, and requires all core measured slices to meet the qualification floor.
- The same deterministic seed can now be rerun to establish a trustworthy runtime-calibration baseline before production classifier changes are considered.


## V11.2 truth hardening and runtime topic calibration

- An initial exact-topic oracle is emitted only when its compact primary anchor remains recoverable after truncation and keyboard-layout mutation; otherwise visible topics plus the support fallback form the ambiguity allowlist.
- Position-aware named topic anchors keep the user's explicit subject ahead of incidental VIP, Ads and QCoin state.
- Mixed Latin/Cyrillic keyboard-layout corruption is repaired only in an alternate semantic view; the original message is never rewritten.
- Compound continuation requests preserve a real established primary topic; an ambiguity-only support fallback yields to the first explicit named topic while secondary topics remain in the hypothesis set.
- Contact/partnership and Deep Translate/localization are first-class production topics.
- Word-boundary and domain-specific aliases prevent false Quest/Academy matches and distinguish GeoDetect, moderation corrections and support-social acts.
- Multi-intent probes name both expected topics explicitly, GeoDetect seeds remain inside feed geolocation, and named topic evidence overrides stale ambiguity in topic-switch arbitration.

- V11.2.1 preserves explicit routed topics when a flattened route object is reframed; an absent `intent.top.topic` no longer normalizes to `support_system` and overrides text hints.

## V11.2.2 explicit topic rejection calibration

- A topic mention preceded by an explicit rejection such as `Не про QCoin` or `not about QCoin` is no longer scored as the active named topic.
- The rejected stale topic is removed from positive alias, native-status, catalog fallback and previous-focus scoring unless the same topic is mentioned positively elsewhere in the message.
- Inclusive constructions such as `not only QCoin` remain positive and continue to participate in multi-intent routing.
- Regression coverage proves that `Не про QCoin. Теперь покажи мои рекламные метрики.` switches to `ads_campaigns`.


## V11.3 evidence-bound oracle and multilingual routing calibration

- Partnership requests are represented as `contact + domain` only when partnership language remains visible after truncation; a lost business-intent label cannot manufacture an exact contact oracle.
- Ambiguity allowlists inherit previously established oracle candidates and include bounded operational fallbacks, preventing one uncertain opening from cascading false failures through deictic follow-ups.
- Strong topic anchors and weak truncated signals are separated: partial fragments may be accepted as ambiguity candidates but never promoted to exact-topic proof.
- Generic phrases such as `help`, `I changed the subject`, and `do not repeat` no longer masquerade as exact `support_system` evidence.
- Runtime routing recognizes multilingual partnership language, moderation appeals and report-deletion phrases, Ukrainian exchange stems, Russian/Ukrainian advertising packages, roadmap/future-plan wording, Quantum Family over incidental VIP state, and a bare explicit Push subject over a balance joke.
- Unicode-safe `invest` boundaries prevent Spanish `Investígalo` from being misclassified as an investment/partnership request.
- Regression evidence replays all 399 non-critical V11.2.3 failure scenarios with the same production-baseline seed and requires zero routing/oracle failures before delivery.

## V11.3 residual evidence closure

The V11.3 calibration is based on the deterministic V11.2.3 corpus run with 10,000 scenarios and 155,211 turns. It separates production routing defects from oracle defects instead of weakening acceptance thresholds.

The residual closure adds:

- multi-topic deictic continuation that preserves every established topic instead of forcing the partnership/contact primary topic on later generic follow-ups;
- multilingual GeoDetect geographic-feed evidence;
- explicit privacy/data-handling and account-deletion/data-cleanup evidence;
- MetaMarket item-price-growth safety wording;
- weak-only treatment for a bare or truncated `forum` token, preventing unsupported exact expectations;
- regression scenarios for the prior failure corpus and multilingual routing matrix.

The calibration remains offline by default. It does not write MongoDB business data, does not call provider translation unless explicitly enabled, and does not auto-promote learned behavior.

## V11.3.1 MetaMarket item-price evidence hotfix

- Safety wording about an item price growing, rising, increasing or going up is treated as MetaMarket evidence in English, Russian and Ukrainian even when the brand name is omitted.
- The simulation oracle uses the same bounded evidence, so an explicit MetaMarket price-safety turn is not incorrectly graded as unresolved ambiguity.
- The matcher is bounded to item/price language and does not steal explicit QCoin token-price or Futures price-growth requests.
- Unit and integration regressions cover the exact failed English and Russian cases plus QCoin/Futures negative controls.


## V11.4.0 semantic and emotional breadth

- `semanticNuanceV11` separates advertising package lifecycle, campaign metrics, purchase, benefits and campaign creation/status across bounded multilingual and slang variants.
- Explicit metrics rejection cannot be scored as a metrics request; QCoin balance, wallet connection, payment/VIP state and moderation privacy/appeal/count intents have independent sub-intents.
- `emotionalPresentationV11` maps bounded emotional evidence to signed card theme, glyph, intensity and reduced-motion-safe pulse without changing facts, permissions or business data.
- Choice cards render at most four signed domain options plus signed `Other`, vertically and at full width.
- Support administrator HTML contains one direct-DM action and a Gmail-safe high-contrast user-claim surface.
- `v11-conversation-breadth-audit.mjs` measures deterministic semantic, emotion and 43-topic × 32-language response breadth separately from live/provider/Mongo/browser acceptance.
- Finite simulation is evidence for the measured corpus, not a claim that every possible natural-language utterance has been proven.

## V11.4.1 evidence-bound actor read

- `conversationIntelligence.js` recognises bounded actor-scoped reads after a topic switch.
- Regression evidence covers `VIP -> QCoin` for «покажи мой баланс».
- The decision remains `topic_switch_v9`, while `caseStatus=ready_for_diagnostic` and `shouldDiagnose=true`.
- The guard is read-only, requires an explicit multilingual self-reference and `user_safe_evidence_only`, and rejects generic informational balance/metrics questions.

## V11.4.2 opaque identifier evidence

Coverage now includes static, self-test and integration proofs that `user-7`, `case-7`, `msg-7` and `corr-7` survive administrator report composition without title-casing or space encoding, while the email still exposes exactly one direct-DM action and no legacy operator buttons.

## V11.5.0 human conversation / instant entry / live-read coverage

Coverage includes:

- at least eight entry-greeting variants for every core locale and recent-variant avoidance;
- optimistic-client/server-variant parity, exact transient cleanup and newest-first rendering contract;
- one bounded idle nudge after Support silence, with timer cancellation and no automatic diagnostic start;
- social-act classification across eight core languages plus spacing, emoji, punctuation, line-break, politeness and filler mutations;
- material requests containing greetings, slang, profanity or social prefixes without losing the real topic;
- multi-turn social-to-material transitions and repeated casual-chat conversion to four signed options plus `Other`;
- identity/purpose wording that names Quantum L7 AI Global only in the identity branch;
- forbidden internal-language and irrelevant-ID-prompt checks;
- same-source Ads package/campaign reads, active-package-with-zero-campaign evidence, VIP/QCoin read-only boundaries and exact Support-evidence cleanup;
- explicit provider and authenticated CDP modes with independent evidence reports.

The deterministic human-conversation corpus is not presented as proof of every possible utterance. Its expectations encode the intended human response policy and are supplemented by provider, Mongo and browser evidence when those modes are explicitly enabled.


## V11.6.0 coverage

- 43 semantic domains × 24 goals = 1032 catalogued micro-intents.
- Screenshot regressions: AI Box usage/accuracy, QCoin theft, greeting reset, Ads ELITE with zero available campaigns.
- Evidence status gate and machine-field leak guard.
- 30 vector-only semantic badges with reduced-motion support.
- Quick/Full/Deep/Extreme premium simulation modes: 25k/250k/1m/5m.
- Dedicated premium evidence and SVG-language audits.
- Canonical same-source Mongo readers, provider translation and CDP browser acceptance remain separately measured runtime evidence.
