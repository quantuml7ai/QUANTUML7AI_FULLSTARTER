export default function buildSearchSortControlsProps({
  t,
  locale,
  q,
  setQ,
  openOnly,
  drop,
  setDrop,
  results,
  headAutoOpenRef,
  setHeadPinned,
  setHeadHidden,
  getScrollEl,
  data,
  pushNavState,
  setTopicFilterId,
  setSel,
  setThreadRoot,
  openThreadForPost,
  sortOpen,
  setSortOpen,
  videoFeedOpen,
  setVideoFeedUserSortLocked,
  setFeedSort,
  feedSort,
  topicSort,
  postSort,
  sel,
  inboxOpen,
  inboxTab,
  profileBranchMode,
  threadRoot,
  setPostSort,
  setTopicSort,
  starMode,
  setStarMode,
  questEnabled,
  questBtnClass,
  openQuests,
  onCommitSortChange,
}) {
  return {
    t,
    locale,
    q,
    setQ,
    openOnly,
    drop,
    setDrop,
    results,
    headAutoOpenRef,
    setHeadPinned,
    setHeadHidden,
    getScrollEl,
    data,
    pushNavState,
    setTopicFilterId,
    setSel,
    setThreadRoot,
    openThreadForPost,
    sortOpen,
    setSortOpen,
    videoFeedOpen,
    setVideoFeedUserSortLocked,
    setFeedSort,
    feedSort,
    topicSort,
    postSort,
    sel,
    profileBranchMode,
    forcePostSort:
      profileBranchMode === 'posts' ||
      (inboxOpen && String(inboxTab || 'messages') !== 'messages'),
    sortControlsDisabled:
      Boolean(inboxOpen && String(inboxTab || 'messages') === 'messages'),
    geoSortAllowed:
      Boolean(videoFeedOpen) ||
      (!threadRoot && !profileBranchMode && !inboxOpen),
    setPostSort,
    setTopicSort,
    starMode,
    setStarMode,
    questEnabled,
    questBtnClass,
    openQuests,
    onCommitSortChange,
  }
}
