import { describe,it,expect } from 'vitest';import fs from 'node:fs';const r=p=>fs.readFileSync(p,'utf8');describe('QL7 Support canonical permanent contracts',()=>{it('uses premium Card canonical and semantic tables',()=>{const c=r('app/forum/features/dm/components/Ql7SupportCard.js');expect(c).toContain('adaptQl7SupportCardForRender');expect(c).toContain('ql7SupportDataTable');expect(c).toContain('ql7SupportMetricGrid')});it('has entry greeting coordinator and API',()=>{expect(fs.existsSync('app/api/dm/support-entry/route.js')).toBe(true);expect(r('app/forum/ForumRoot.jsx')).toContain('/api/dm/support-entry')});it('uses authoritative VIP side-effect-free resolver and identity graph',()=>{expect(r('lib/ql7-support/vipResolver.js')).toContain('isVipNowSideEffectFree');expect(r('lib/ql7-support/identityGraph.js')).toContain('account_aliases')});it('keeps Uzbek provider-backed, not core',()=>{const p=r('lib/ql7-support/presentation.js');expect(p).not.toMatch(/CORE_LOCALES[^\n]+uz/);expect(r('lib/ql7-support/languageOrchestrator.js')).toContain("return 'uz'")});it('requires transport receipt before sent and exposes reconciliation',()=>{const e=r('lib/ql7-support/emailOutboxWorker.js');expect(e).toContain('email_transport_receipt_missing');expect(e).toContain('reconcileQl7SupportEmailOutbox')});it('has governed learning with opt-out/delete',()=>{const l=r('lib/ql7-support/learningGovernance.js');expect(l).toContain('setQl7LearningOptOut');expect(l).toContain('deleteQl7LearningProfile');expect(l).toContain('productionMutation:false')})})
