import { describe,it,expect } from 'vitest';import fs from 'node:fs';const r=p=>fs.readFileSync(p,'utf8');describe('QL7 Support V8 permanent contracts',()=>{it('uses premium Card V3 and semantic tables',()=>{const c=r('app/forum/features/dm/components/Ql7SupportCard.js');expect(c).toContain('adaptQl7SupportCardForRenderV3');expect(c).toContain('ql7SupportDataTable');expect(c).toContain('ql7SupportMetricGrid')});it('has entry greeting coordinator and API',()=>{expect(fs.existsSync('app/api/dm/support-entry/route.js')).toBe(true);expect(r('app/forum/ForumRoot.jsx')).toContain('/api/dm/support-entry')});it('uses authoritative VIP side-effect-free resolver and identity graph',()=>{expect(r('lib/ql7-support/vipResolverV8.js')).toContain('isVipNowSideEffectFree');expect(r('lib/ql7-support/identityGraphV8.js')).toContain('account_aliases')});it('keeps Uzbek provider-backed, not core',()=>{const p=r('lib/ql7-support/presentationV8.js');expect(p).not.toMatch(/CORE_LOCALES[^\n]+uz/);expect(r('lib/ql7-support/languageOrchestrator.js')).toContain("return 'uz'")});it('requires transport receipt before sent and exposes reconciliation',()=>{const e=r('lib/ql7-support/emailOutboxWorker.js');expect(e).toContain('email_transport_receipt_missing');expect(e).toContain('reconcileQl7SupportEmailOutboxV8')});it('has governed learning with opt-out/delete',()=>{const l=r('lib/ql7-support/learningGovernanceV8.js');expect(l).toContain('setQl7LearningOptOutV8');expect(l).toContain('deleteQl7LearningProfileV8');expect(l).toContain('productionMutation:false')})})
