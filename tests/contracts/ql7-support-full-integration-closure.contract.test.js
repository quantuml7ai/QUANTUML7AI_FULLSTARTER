import {describe,expect,it} from 'vitest'
import {auditQl7AdsExpirationPolicy} from '../../lib/ql7-support/knowledge/adsReadPolicy.js'
import {QL7_SUPPORT_NOVELTY_RESERVATION_POLICY,buildQl7SupportNoveltyReservationDescriptors} from '../../lib/ql7-support/response/noveltyReservation.js'
import {QL7_SUPPORT_MAX_REGENERATION_ATTEMPTS,QL7_SUPPORT_SAFE_FALLBACK_STRATEGY_BUDGET} from '../../lib/ql7-support/response/regenerationController.js'
describe('QL7 Support full integration closure contract',()=>{it('executes current Ads expiration and novelty owners',()=>{expect(auditQl7AdsExpirationPolicy().ok).toBe(true);expect(QL7_SUPPORT_NOVELTY_RESERVATION_POLICY.semanticIdentityIsExclusive).toBe(false);expect(QL7_SUPPORT_NOVELTY_RESERVATION_POLICY.durableReservationScope).toBe('actor_conversation_turn');expect(QL7_SUPPORT_MAX_REGENERATION_ATTEMPTS).toBe(16);expect(QL7_SUPPORT_SAFE_FALLBACK_STRATEGY_BUDGET).toBe(32);const common={actorIdHash:'contract-actor',conversationId:'contract-conversation',locale:'en',scopeReceipt:{primaryDomainId:'forum',primaryMicrotopicId:'forum.explain',selectedIntentId:'explain'},semanticPlan:{planHash:'contract-plan'},qualityGate:{novelty:{fingerprint:{exactHash:'x',normalizedHash:'n',unorderedSentenceMultisetHash:'s',unorderedClauseMultisetHash:'c',rhetoricalSkeletonHash:'r',openingHash:'o',closingHash:'z',titleHash:'t',sentenceHashes:['s1'],clauseHashes:['c1'],immutableSentenceHashes:[],immutableClauseHashes:[],minHashSignature:['m1']}}}};const a=buildQl7SupportNoveltyReservationDescriptors({...common,turnId:'a'}),b=buildQl7SupportNoveltyReservationDescriptors({...common,turnId:'b'}),r=buildQl7SupportNoveltyReservationDescriptors({...common,turnId:'a'}),ids=new Set(a.map(x=>x.reservationId));expect(b.filter(x=>ids.has(x.reservationId))).toHaveLength(0);expect(r.filter(x=>ids.has(x.reservationId))).toHaveLength(a.length)})})
