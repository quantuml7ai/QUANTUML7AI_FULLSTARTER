import { describe,it,expect } from 'vitest'
import { QL7_SUPPORT_CORE_LOCALES_V8,adaptQl7SupportCardForRenderV3,localizeQl7MetricV8,localizeQl7StatusV8,ql7SupportLabelsV8 } from '../../../lib/ql7-support/presentationV8.js'
import { detectQl7SupportLanguage } from '../../../lib/ql7-support/languageOrchestrator.js'
import { buildQl7SupportCardV3,validateQl7SupportCardAny } from '../../../lib/ql7-support/cardSchemaV3.js'
describe('QL7 Support V8 cognitive/presentation',()=>{it('keeps only ecosystem i18n locales as core and routes Uzbek to provider',()=>{expect(QL7_SUPPORT_CORE_LOCALES_V8).toEqual(['en','ru','uk','es','tr','ar','zh']);expect(detectQl7SupportLanguage('Birjada AI boksdan qanday foydalanish mumkin?','en')).toBe('uz')});it('localizes raw metrics and status',()=>{expect(localizeQl7MetricV8('PackageCount','ru')).toBe('Пакетов найдено');expect(localizeQl7StatusV8('noData','uk')).not.toMatch(/noData/i);expect(ql7SupportLabelsV8('ar').confirmed).toBe('حقائق مؤكدة')});it('signs and validates Card V3',()=>{const c=buildQl7SupportCardV3({purpose:'complaint',locale:'ru',title:'Жалоба',summary:'Проверено',status:'human_review',metrics:{currentReports:1}});expect(c.version).toBe(3);expect(validateQl7SupportCardAny(c).ok).toBe(true)});it('preserves legacy V2 table timeline content type and provider direction without fact duplication',()=>{const x=adaptQl7SupportCardForRenderV3({version:2,kind:'moderation_snapshot',locale:'ru',title:'Жалоба',integrity:'a'.repeat(64),snapshot:{contentType:'image'},facts:[{label:'Статус',value:'Активен'}],table:{columns:[{key:'metric',label:'Метрика'},{key:'value',label:'Значение'}],rows:[{metric:'CTR',value:'4.2%'}]},timeline:[{label:'Получено',asOf:'2026-07-24T10:00:00.000Z'}]});expect(x.snapshot.contentType).toBe('Изображение');expect(x.metrics.map(v=>v.value)).toEqual(['CTR','4.2%']);expect(x.timeline[0].value).toBe('Получено');expect(x.sections).toHaveLength(1);expect(x.metrics.some(v=>v.value==='Активен')).toBe(false);const he=adaptQl7SupportCardForRenderV3({version:2,kind:'diagnostic',locale:'he',title:'Result',integrity:'b'.repeat(64)});expect(he.locale).toBe('he');expect(he.direction).toBe('rtl')})})
