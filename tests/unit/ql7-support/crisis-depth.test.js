import {describe,it,expect} from 'vitest';import {auditQl7CrisisDepth} from '../../../lib/ql7-support/simulation/crisisDepthOracle.js';describe('canonical crisis depth',()=>{it('covers 32 locales with 64 contextual cue records each and no diagnosis authority',()=>{const r=auditQl7CrisisDepth();expect(r.ok).toBe(true);expect(r.cues.localeCount).toBe(32);expect(r.cues.cuesPerLocale).toBeGreaterThanOrEqual(64);expect(r.cues.totalCues).toBeGreaterThanOrEqual(2048);expect(r.noDiagnosis).toBe(true)})})
