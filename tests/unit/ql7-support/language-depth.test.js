import {describe,it,expect} from 'vitest';import {auditQl7LanguageDepth} from '../../../lib/ql7-support/simulation/languageSeedDiversityOracle.js';describe('canonical language depth',()=>{it('has 32 locales with 1500 code-reviewed non-mutation seeds, 256 dialect/register families and 24 mutations',()=>{const r=auditQl7LanguageDepth();expect(r.ok).toBe(true);expect(r.seeds.localeCount).toBe(32);expect(r.seeds.seedsPerLocale).toBeGreaterThanOrEqual(1500);expect(r.variants.dialectFamilyCount).toBeGreaterThanOrEqual(256);expect(r.variants.mutationFamilyCount).toBeGreaterThanOrEqual(24);expect(r.semantic.totalTerms).toBeGreaterThanOrEqual(250000)})})
