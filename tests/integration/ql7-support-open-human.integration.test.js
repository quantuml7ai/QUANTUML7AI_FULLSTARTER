import {describe,it,expect} from 'vitest';import {resolveQl7OpenHumanConcept} from '../../lib/ql7-support/knowledge/openHumanKnowledgeRouter.js';describe('canonical open human source route',()=>{it('does not turn unknown ordinary topic into a dead menu fallback',()=>{const r=resolveQl7OpenHumanConcept({topicFamilyId:'open_subject',facetId:'overview'});expect(r.openSubject).toBe(true);expect(r.sourceRequired).toBe(true);expect(r.stableSemanticContextAllowed).toBe(true)})})
