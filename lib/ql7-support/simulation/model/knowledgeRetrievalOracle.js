import crypto from 'node:crypto'
const h=v=>crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex')
export const QL7_KNOWLEDGE_RETRIEVAL_ORACLE_VERSION='1.0.0'
export function evaluateQl7KnowledgeRetrieval({retrieved=[],relevantClaimIds=[],forbiddenClaimIds=[]}={}){const got=new Set((retrieved||[]).map(x=>String(x.claimId||x.id||'')));const rel=[...relevantClaimIds].map(String),forbidden=[...forbiddenClaimIds].map(String);const recall=rel.length?rel.filter(x=>got.has(x)).length/rel.length:1;const leakage=forbidden.filter(x=>got.has(x));const body={schema:'ql7.support.oracle.knowledge-retrieval',schemaVersion:QL7_KNOWLEDGE_RETRIEVAL_ORACLE_VERSION,ok:recall===1&&!leakage.length,recallAtK:recall,forbiddenLeakage:Object.freeze(leakage),retrievedCount:got.size};return Object.freeze({...body,receiptHash:h(body)})}
