import {ql7Arr,ql7StableHash,ql7Str} from '../../internal/text.js'
export function createQl7KnowledgeSnapshotManifest(x={}){const body={schema:'ql7.knowledge.snapshot',schemaVersion:1,snapshotId:ql7Str(x.snapshotId),sourceRegistryHash:ql7Str(x.sourceRegistryHash),claimCount:Math.max(0,Number(x.claimCount||0)),entityCount:Math.max(0,Number(x.entityCount||0)),indexHashes:Object.freeze(ql7Arr(x.indexHashes)),createdAt:ql7Str(x.createdAt),signed:x.signed===true};return Object.freeze({...body,snapshotHash:ql7StableHash(JSON.stringify(body))})}
