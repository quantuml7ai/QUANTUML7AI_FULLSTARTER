export function resolveQl7KnowledgeConflicts(rows=[]){const by=new Map;for(const r of rows){const k=String(r.claimKey||r.claimId||'');if(!by.has(k))by.set(k,[]);by.get(k).push(r)}const accepted=[],conflicts=[];for(const [k,v] of by){if(v.length===1)accepted.push(v[0]);else{const vals=new Set(v.map(x=>JSON.stringify(x.value??x.proposition)));if(vals.size===1)accepted.push(v.sort((a,b)=>Number(b.sourceScore||0)-Number(a.sourceScore||0))[0]);else conflicts.push({claimKey:k,rows:v})}}return Object.freeze({accepted:Object.freeze(accepted),conflicts:Object.freeze(conflicts)})}
