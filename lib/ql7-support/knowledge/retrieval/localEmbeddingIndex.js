export function cosineQl7(a=[],b=[]){let dot=0,aa=0,bb=0;for(let i=0;i<Math.max(a.length,b.length);i++){const x=Number(a[i]||0),y=Number(b[i]||0);dot+=x*y;aa+=x*x;bb+=y*y}return aa&&bb?dot/Math.sqrt(aa*bb):0}
export function searchQl7LocalEmbeddingIndex(vector=[],rows=[],k=16){return rows.map(r=>({...r,embeddingScore:cosineQl7(vector,r.vector||[])})).sort((a,b)=>b.embeddingScore-a.embeddingScore).slice(0,k)}
