export function buildQl7SecurityReadPlan(frame={}){const sources=['auth_session'];if(/asset|payment|qcoin|wallet/iu.test(frame.eventType||''))sources.push('economic_receipts');if(/device|session|login/iu.test(frame.eventType||''))sources.push('session_security');return Object.freeze({schema:'ql7.security.read-plan',schemaVersion:1,sources:Object.freeze(sources),writeAllowed:false})}
