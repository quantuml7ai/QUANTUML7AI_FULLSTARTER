export function decideQl7SecurityAction(risk={}){if(risk.deterministicCompromise)return Object.freeze({action:'QUARANTINE_ACCOUNT',requiresDeterministicReceipt:true});if(risk.objectiveProofCount>=1)return Object.freeze({action:'STEP_UP_REQUIRED',requiresDeterministicReceipt:true});return Object.freeze({action:'ALLOW',requiresDeterministicReceipt:false})}
