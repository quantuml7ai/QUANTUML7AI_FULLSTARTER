export function decideQl7AssetProtection({authorized=false,receiptVerified=false,compromise=false}={}){if(!authorized)return Object.freeze({action:'REJECT_OPERATION',reason:'authorization_required'});if(compromise&&receiptVerified)return Object.freeze({action:'REJECT_OPERATION',reason:'deterministic_compromise'});return Object.freeze({action:'ALLOW'})}
