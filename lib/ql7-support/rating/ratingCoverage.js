export function ql7RatingCoverage(rows=[]){const den=rows.reduce((s,r)=>s+Math.abs(Number(r.weight||0)),0),num=rows.reduce((s,r)=>s+Math.abs(Number(r.weight||0))*Number(r.evidenceQuality||0),0);return den?num/den:0}
