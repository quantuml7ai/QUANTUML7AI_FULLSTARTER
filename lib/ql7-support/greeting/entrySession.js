import crypto from 'node:crypto'
export const QL7_SUPPORT_ENTRY_SESSION_VERSION='5.4.0'
const sha=v=>crypto.createHash('sha256').update(String(v)).digest('hex')
export function deriveQl7EntrySession({actorHash='',conversationId='ql7-support',entrySessionId='',entryEpoch='0',locale='en',openedAt=new Date().toISOString(),reopenReason='open',greetingPolicyVersion='5.4.0'}={}){const stable=String(entrySessionId||sha(`${actorHash}:${conversationId}:${entryEpoch}:${locale}`).slice(0,32)),body={schema:'ql7.support.entry-session',schemaVersion:QL7_SUPPORT_ENTRY_SESSION_VERSION,actorHash:String(actorHash),conversationId:String(conversationId),entrySessionId:stable,entryEpoch:String(entryEpoch),openedAt,reopenReason,locale,greetingPolicyVersion,greetingReservationId:`greeting:${sha(`${actorHash}:${conversationId}:${stable}`).slice(0,32)}`,greetingDeliveryId:'',committed:false};return Object.freeze({...body,receiptHash:sha(JSON.stringify(body))})}
export function commitQl7EntryGreeting(session,{greetingDeliveryId=''}={}){const body={...session,greetingDeliveryId:String(greetingDeliveryId),committed:true};delete body.receiptHash;return Object.freeze({...body,receiptHash:sha(JSON.stringify(body))})}
export function auditQl7EntrySession(){const a=deriveQl7EntrySession({actorHash:'a',conversationId:'c',entrySessionId:'tab-session'}),b=deriveQl7EntrySession({actorHash:'a',conversationId:'c',entrySessionId:'tab-session'}),c=deriveQl7EntrySession({actorHash:'a',conversationId:'c',entrySessionId:'another'});const ok=a.greetingReservationId===b.greetingReservationId&&a.greetingReservationId!==c.greetingReservationId;return Object.freeze({ok,exactlyOnceKeyStable:ok,failures:Object.freeze(ok?[]:['greeting_key_instability'])})}
