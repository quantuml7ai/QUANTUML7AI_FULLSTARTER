const SECRET=/(?:seed phrase|private key|session token|password|mnemonic)/iu
export function projectQl7SafeUserData(value){if(value==null)return value;if(typeof value==='string')return SECRET.test(value)?'[REDACTED]':value;if(Array.isArray(value))return value.map(projectQl7SafeUserData);if(typeof value==='object'){const out={};for(const [k,v] of Object.entries(value)){if(/(?:secret|token|privatekey|seed|password)/iu.test(k))continue;out[k]=projectQl7SafeUserData(v)}return Object.freeze(out)}return value}
