[project]
name = "ql7-native"
version = "0.2.0"
description = "QL7-owned native multilingual intelligence training and evaluation stack"
requires-python = ">=3.11,<3.14"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=9,<10", "numpy>=2,<3"]

[tool.setuptools.packages.find]
where=["src"]
